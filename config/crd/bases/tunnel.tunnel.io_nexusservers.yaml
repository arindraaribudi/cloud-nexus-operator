---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nexusservers.tunnel.tunnel.io
spec:
  group: tunnel.tunnel.io
  names:
    kind: NexusServer
    listKind: NexusServerList
    plural: nexusservers
    singular: nexusserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.address
      name: Address
      type: string
    - jsonPath: .status.port
      name: Port
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NexusServer is the Schema for the nexusservers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NexusServerSpec defines the desired state of NexusServer.
            properties:
              authTokenSecret:
                description: |-
                  AuthTokenSecret is the secret containing the auth token frpc must present.
                  When nil, the operator creates a random token secret automatically.
                properties:
                  key:
                    description: Key is the key within the Secret.
                    type: string
                  name:
                    description: Name is the name of the Secret.
                    type: string
                required:
                - key
                - name
                type: object
              bindPort:
                default: 7000
                format: int32
                type: integer
              dashboardPasswordSecret:
                description: DashboardPasswordSecret is the secret containing the
                  dashboard password. Optional.
                properties:
                  key:
                    description: Key is the key within the Secret.
                    type: string
                  name:
                    description: Name is the name of the Secret.
                    type: string
                required:
                - key
                - name
                type: object
              dashboardPort:
                description: DashboardPort is the frps dashboard port. Optional.
                format: int32
                type: integer
              dashboardUser:
                description: DashboardUser is the username for the frps dashboard.
                  Optional.
                type: string
              discoveryPort:
                description: |-
                  DiscoveryPort is the port the nexus-discovery sidecar listens on inside
                  the frps pod. When non-zero, a nexus-discovery sidecar is injected into
                  the frps Deployment and this port is added to the frps LoadBalancer Service.
                  Spoke NexusClients with serverRef.discoveryPort set query this endpoint
                  to auto-discover the hub gateway FQDN and vhostHTTPPort.
                format: int32
                type: integer
              enableOperatorPlugin:
                description: |-
                  EnableOperatorPlugin enables the frps httpPlugin that notifies the
                  operator when proxies connect/disconnect. Required for RegistryProxy.
                  Default: false
                type: boolean
              extraConfig:
                description: ExtraConfig is raw TOML appended verbatim to frps.toml.
                type: string
              gatewayServiceType:
                default: ClusterIP
                description: |-
                  GatewayServiceType is the K8s Service type for the unified HTTP gateway.
                  The gateway Service is only created when vhostHTTPPort is non-zero.
                type: string
              image:
                description: Image specifies the container image for frps.
                properties:
                  repository:
                    default: asia-southeast3-docker.pkg.dev/crd-operations/crd-gitops/crd-tunnel
                    type: string
                  tag:
                    default: 1.0.0
                    type: string
                type: object
              operatorWebhookAddr:
                description: |-
                  OperatorWebhookAddr is the address of the operator frps-plugin HTTP server.
                  Only used when EnableOperatorPlugin is true.
                  Example: "cloud-nexus-operator-controller-manager-frps-plugin.cloud-nexus-operator-system.svc.cluster.local:9090"
                  If empty, defaults to "controller-manager-frps-plugin.<namespace>.svc.cluster.local:9090"
                  where <namespace> is the NexusServer's namespace.
                type: string
              replicas:
                default: 1
                format: int32
                type: integer
              service:
                description: Service describes the Kubernetes Service that exposes
                  frps.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are passed verbatim to the Service metadata.
                    type: object
                  ports:
                    description: Ports defines the ports to expose.
                    items:
                      description: ServicePort contains information on service's port.
                      properties:
                        appProtocol:
                          description: |-
                            The application protocol for this port.
                            This is used as a hint for implementations to offer richer behavior for protocols that they understand.
                            This field follows standard Kubernetes label syntax.
                            Valid values are either:

                            * Un-prefixed protocol names - reserved for IANA standard service names (as per
                            RFC-6335 and https://www.iana.org/assignments/service-names).

                            * Kubernetes-defined prefixed names:
                              * 'kubernetes.io/h2c' - HTTP/2 prior knowledge over cleartext as described in https://www.rfc-editor.org/rfc/rfc9113.html#name-starting-http-2-with-prior-
                              * 'kubernetes.io/ws'  - WebSocket over cleartext as described in https://www.rfc-editor.org/rfc/rfc6455
                              * 'kubernetes.io/wss' - WebSocket over TLS as described in https://www.rfc-editor.org/rfc/rfc6455

                            * Other protocols should use implementation-defined prefixed names such as
                            mycompany.com/my-custom-protocol.
                          type: string
                        name:
                          description: |-
                            The name of this port within the service. This must be a DNS_LABEL.
                            All ports within a ServiceSpec must have unique names. When considering
                            the endpoints for a Service, this must match the 'name' field in the
                            EndpointPort.
                            Optional if only one ServicePort is defined on this service.
                          type: string
                        nodePort:
                          description: |-
                            The port on each node on which this service is exposed when type is
                            NodePort or LoadBalancer.  Usually assigned by the system. If a value is
                            specified, in-range, and not in use it will be used, otherwise the
                            operation will fail.  If not specified, a port will be allocated if this
                            Service requires one.  If this field is specified when creating a
                            Service which does not need it, creation will fail. This field will be
                            wiped when updating a Service to no longer need it (e.g. changing type
                            from NodePort to ClusterIP).
                            More info: https://kubernetes.io/docs/concepts/services-networking/service/#type-nodeport
                          format: int32
                          type: integer
                        port:
                          description: The port that will be exposed by this service.
                          format: int32
                          type: integer
                        protocol:
                          default: TCP
                          description: |-
                            The IP protocol for this port. Supports "TCP", "UDP", and "SCTP".
                            Default is TCP.
                          type: string
                        targetPort:
                          anyOf:
                          - type: integer
                          - type: string
                          description: |-
                            Number or name of the port to access on the pods targeted by the service.
                            Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
                            If this is a string, it will be looked up as a named port in the
                            target Pod's container ports. If this is not specified, the value
                            of the 'port' field is used (an identity map).
                            This field is ignored for services with clusterIP=None, and should be
                            omitted or set equal to the 'port' field.
                            More info: https://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service
                          x-kubernetes-int-or-string: true
                      required:
                      - port
                      type: object
                    type: array
                  type:
                    default: LoadBalancer
                    description: Service Type string describes ingress methods for
                      a service
                    type: string
                type: object
              vhostHTTPPort:
                description: VhostHTTPPort is the port for HTTP virtual host. Optional.
                format: int32
                type: integer
              vhostHTTPSPort:
                description: VhostHTTPSPort is the port for HTTPS virtual host. Optional.
                format: int32
                type: integer
            type: object
          status:
            description: NexusServerStatus defines the observed state of NexusServer.
            properties:
              address:
                description: Address is the external IP or hostname of the frps Service.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: Phase is the current lifecycle phase.
                type: string
              port:
                description: Port is the bind port of frps.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
